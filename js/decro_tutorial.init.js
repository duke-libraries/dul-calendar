// Javascript for dul_calendar module
(function($) {
	Drupal.behaviors.dul_calendar = {
		attach : function(context, settings) {
			
			// the settings that were created in the 
			// page callback are accessible in this manner
			s = settings.dul_calendar;
			
		}
	}	
})(jQuery);
